# This is an instance of the master vagrant box which will build actual containers
Vagrant.configure("2") do |config|
  config.vm.box = "SingularityBuilder"
  config.ssh.insert_key = false

  config.vm.provider "virtualbox" do |v|
    v.memory = 1024
    v.cpus = 2
  end
end
